<Window x:Class="TaggedImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaggedImageViewer"
        mc:Ignorable="d"
        Title="Drawings Browser" Width="800" Height="600">
    <Grid>
        <Grid.ColumnDefinitions>
             <ColumnDefinition Width="*"/> 
             <ColumnDefinition Width="5"/> 
             <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <ListView Name="DirectoryListBox" SelectionChanged="DirectoryListBox_OnSelectionChanged" HorizontalAlignment="Stretch">
            <ListView.ItemsPanel> 
                <ItemsPanelTemplate>
                     <UniformGrid Columns="3" HorizontalAlignment="Left" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type local:DirectoryItem}">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="125">
                        <Image Source="{Binding Thumbnail}" Width="100" Height="100" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding DisplayName}" HorizontalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Gray" />
        
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <TextBlock Text="XCF Files"></TextBlock>
                <ListView Name="XcfFilesListBox" ScrollViewer.CanContentScroll="False">
                    <ListView.ItemsPanel> 
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" HorizontalAlignment="Left" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:DirectoryItem}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="125">
                                <Image Source="{Binding Thumbnail}" Width="100" Height="100" HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding DisplayName}" HorizontalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            
                <TextBlock Text="Image Files"></TextBlock>
                <ListView Name="ImgListBox" ScrollViewer.CanContentScroll="False">
                    <ListView.ItemsPanel> 
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" HorizontalAlignment="Left" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:DirectoryItem}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="125">
                                <Image Source="{Binding Thumbnail}" Width="100" Height="100" HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding DisplayName}" HorizontalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>